import React from 'react';
import Header from '../components/header';

function ProjectsInfo(){
    return(
        <div>
            <Header/>
            <h1>My project's info</h1>
        </div>
    );
};

export default ProjectsInfo;